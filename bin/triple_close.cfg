;
; Chaos configuration file
; Simulation of magnets and pendulum
;
[FIELD]
WIDTH=1000
HEIGHT=1000

[SIMULATION]
FRICTION=0.0003
PEND_HEIGHT=10
DELTA_T=2
MIN_STEPS=1000
MAX_STEPS=80000
ABORT_VEL=0.1
;COLOR_SCHEME=1
;COLOR_SCHEME = 1 - (len / max_len)
;COLOR_SCHEME = 1/(exp(0.0000002*(len*len)))
COLOR_SCHEME = 1/(exp((ln(256/1)/(max_len*max_len))*(len*len)))

;
; This source simulates the pendulum force
;
[SOURCE 1]
TYPE=LINEAR
COLOR=255,255,255
RAD=0
THETA=0
MULT=0.000001
SIZE=0

;
; This sources simulate the magnets
;
[SOURCE 2]
TYPE=INV_SQR
COLOR=255,0,0
RAD=200
THETA=90
MULT=10
SIZE=10

[SOURCE 3]
TYPE=INV_SQR
Color=0,255,0
RAD=200
THETA=210
MULT=10
SIZE=10

[SOURCE 4]
TYPE=INV_SQR
COLOR=0,0,255
RAD=200
THETA=330
MULT=10
SIZE=10